services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
      target: builder
    command: sh -c "npx prisma generate --schema=packages/database/prisma/schema.prisma && npm run build --workspace=@pulse/database && npx prisma db push --schema=packages/database/prisma/schema.prisma && npx prisma db seed --schema=packages/database/prisma/schema.prisma && node apps/api/create-admin.js && npm run dev --workspace=@pulse/api"
    ports:
      - "5000:5000"
    environment:
      - DATABASE_URL=file:/app/data/pulse.db
      - PORT=5000
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=adminpassword
      - JWT_SECRET=pulse-development-secret-key-987654321

    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/api/node_modules
      - /app/apps/web/node_modules
      - /app/packages/database/node_modules
      - pulse_dev_data:/app/data

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
      target: builder
    command: npm run dev --workspace=@pulse/web
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:5000
    volumes:
      - .:/app
      - /app/node_modules
      - /app/apps/api/node_modules
      - /app/apps/web/node_modules
      - /app/packages/database/node_modules
    depends_on:
      - api

volumes:
  pulse_dev_data:
